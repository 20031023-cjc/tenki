<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>世界の天気 / World Weather</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <header>
    <h1 id="title">世界の天気</h1>
    <div class="controls">
      <select id="language-select" aria-label="言語選択 / Language Select">
        <option value="ja" selected>日本語</option>
        <option value="en">English</option>
      </select>
      <button id="theme-toggle" aria-label="テーマ切り替え / Toggle Theme">🌙</button>
    </div>
  </header>

  <main>
    <section class="search-section">
      <div class="search-box">
        <input
          type="text"
          id="city-input"
          placeholder="都市名を入力 / Enter city name"
          autocomplete="off"
          aria-label="都市名入力 / City name input"
        />
        <button id="search-btn" aria-label="検索 / Search">🔍</button>
        <button id="locate-btn" aria-label="現在地を取得 / Get Location">📍</button>
      </div>
      <ul id="autocomplete-list" class="autocomplete-list"></ul>
    </section>

    <section id="weather-display" class="weather-display">
      <!-- 天気カード将在这里生成 -->
    </section>

    <section class="favorites-section">
      <h2 id="favorites-title">お気に入りの都市</h2>
      <div id="favorites-list" class="favorites-list">
        <!-- 收藏城市卡片 -->
      </div>
    </section>
  </main>

  <template id="weather-card-template">
    <div class="weather-card">
      <div class="card-front">
        <h3 class="city-name"></h3>
        <div class="temp"></div>
        <img class="weather-icon" alt="weather icon" />
        <div class="weather-main"></div>
        <div class="recommendation"></div>
      </div>
      <div class="card-back">
        <ul class="details-list">
          <li><strong>風速 / Wind Speed:</strong> <span class="wind"></span> m/s</li>
          <li><strong>湿度 / Humidity:</strong> <span class="humidity"></span> %</li>
          <li><strong>気圧 / Pressure:</strong> <span class="pressure"></span> hPa</li>
          <li><strong>日の出 / Sunrise:</strong> <span class="sunrise"></span></li>
          <li><strong>日の入 / Sunset:</strong> <span class="sunset"></span></li>
        </ul>
        <button class="back-btn">← <span class="back-text">戻る / Back</span></button>
      </div>
      <button class="fav-btn" aria-label="お気に入りに追加 / Add to favorites">⭐</button>
    </div>
  </template>

  <script src="script.js"></script>
</body>
</html>
